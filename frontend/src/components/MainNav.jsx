import React from 'react';
import NewNavbar from './NewNavbar';

// This is a simple wrapper component that uses the NewNavbar component
const MainNav = () => {
  return <NewNavbar />;
};

export default MainNav;
